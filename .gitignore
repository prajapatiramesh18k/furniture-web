node_modules
.next
.vercel
.playwright-mcp
.claude

# Build artifacts
tsconfig.tsbuildinfo
*.log
.DS_Store

# Environment variables (local only — never commit secrets)
.env.local
.env.*.local
.env.development
.env.production
# Keep the template tracked
!.env.example
